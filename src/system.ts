export enum Action {
  MULTI_SUBSCRIBE = "gusher.multi_subscribe",
  SUBSCRIBE = "gusher.subscribe",
  UNSUBSCRIBE = "gusher.unsubscribe",
}

export enum Event {
  MULTI_SUBSCRIBE_SUCCESS = "gusher.multi_subscribe_succeeded",
  SUBSCRIBE_SUCCESS = 'gusher.subscribe_succeeded"',
}
